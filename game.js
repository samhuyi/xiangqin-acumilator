/* =========================================================
 * 《相亲模拟器》微信小游戏入口
 * ========================================================= */

'use strict';

/* ---------------------------------------------------------
 * 数据源配置（接入云端数据库只需改这一处）
 * ---------------------------------------------------------
 * CLOUD_ENV_ID：微信云开发环境 ID。
 *   在微信开发者工具「云开发」控制台左上角可查看/复制。
 *   留空 '' 时：使用当前 AppID 的默认云环境
 *   （若只开通了一个环境，直接留空即可）。
 * ------------------------------------------------------- */
var CLOUD_ENV_ID = 'cloud1-d7gwcey64cad30d91';

var config = {};
if (CLOUD_ENV_ID) {
  config.cloud = { env: CLOUD_ENV_ID };
}

// 主屏幕画布（小游戏里第一个 wx.createCanvas() 即为主画布）
var canvas = wx.createCanvas();

var main = require('./js/main.js');

// 数据源：微信云开发数据库（唯一数据源）。
// 云端数据不完整 / 读取失败时，游戏会显示错误页并列出问题清单，
// 请据此到「云开发」控制台排查（集合导入 / 权限 / 字段）。
main.start(canvas, config);
